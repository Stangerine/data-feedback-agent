{
  "name": "data-feedback-agent",
  "version": "0.1.0",
  "type": "module",
  "description": "数据回流 Agent — 目标检测二次校验",
  "scripts": {
    "agent": "node --import tsx/esm agent.ts",
    "agent:pi": "pi run agent.ts"
  },
  "dependencies": {
    "@earendil-works/pi-ai": "latest",
    "@earendil-works/pi-coding-agent": "latest"
  },
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.9.0"
  }
}
